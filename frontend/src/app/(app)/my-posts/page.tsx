"use client";

/* My Posts — the X side of My Videos: same rows, same rules, same pay. */

import { SubmissionsPage } from "@/components/submissions";

export default function MyPostsPage() {
  return <SubmissionsPage kind="post" />;
}
